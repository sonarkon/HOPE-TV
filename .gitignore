# macOS
.DS_Store

# Real WiFi credentials - never commit these, use data/config.example.txt as a template
**/data/config.txt

# Raw source clips/photos (too large for git, kept locally only - the
# converted 128x128 assets actually used by the firmware live in data/
# and data/slideshow/, and ARE tracked)
hopetv/videoclips/

# Personal German-language working notes, not part of the public docs
hopetv/CODEBUCH.md
